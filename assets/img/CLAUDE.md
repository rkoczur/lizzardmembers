# assets/img/

Static images bundled with the app.

- `default-avatar.svg` — fallback avatar; returned by `getAvatarUrl(null)` when no profile picture is set. Do not rename or move without updating `includes/functions.php:getAvatarUrl()`.
