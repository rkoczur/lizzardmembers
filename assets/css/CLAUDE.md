# assets/css/

Single file: `style.css` — covers all pages (admin + user). No preprocessor, no build step.

Add new styles to the bottom of the relevant section. Keep selectors flat — avoid deep nesting.
Use existing CSS custom properties (`var(--primary)`, `var(--border)`, etc.) rather than hardcoded colors.
